"""migite - the Python side of migite: config, the agent gateway, tickets, paths, and
the LangGraph tools. bash reaches it with `python -m migite.<module>`; the checkout
is on PYTHONPATH (lib/common.sh), so nothing needs a symlink or an install.
"""
